// Points the static frontend at the separately-deployed backend (Render, etc).
// Replace the URL below with your actual backend URL once it's deployed, e.g.
// "https://flora-carbon-ai.onrender.com" (no trailing slash).
window.FLORA_API_BASE = "https://REPLACE-WITH-YOUR-RENDER-URL.onrender.com";
